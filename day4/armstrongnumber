#include<stdio.h>
int main()
{

int n,temp,sum=0,d;
printf("Enter the number:");
scanf("%d",&n);

temp=n;

while(temp>0)
{
    d=temp%10;
    sum=sum+d*d*d;
    temp=temp/10;

}
if(sum==n)
{
    printf("armstrong number:");
}
else{
    printf("not an armstrong number:");
}
return 0;
}
